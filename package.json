{
  "name": "meal-checkin-cloud",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,green \"npm run dev --workspace client\" \"npm run dev --workspace server\"",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server",
    "pm2:start": "pm2 start ecosystem.config.cjs",
    "pm2:restart": "pm2 restart meal-checkin-api --update-env",
    "pm2:stop": "pm2 stop meal-checkin-api",
    "pm2:logs": "pm2 logs meal-checkin-api",
    "vapid:keys": "npx web-push generate-vapid-keys",
    "cf:pages:deploy": "wrangler pages deploy client/dist --project-name meal-check-in --branch main",
    "cf:deploy": "wrangler pages deploy client/dist --project-name meal-check-in --branch main"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "wrangler": "^4.97.0"
  }
}
